-- ============================================================
--  ГРА «Алгоритми та програми» — Підсумкова робота ГР 2 та ГР 3
--  (8 клас, урок 24, 24.12.2025 — останній урок І семестру)
--  Куди: Supabase → SQL Editor → New query → вставити ВСЕ → Run.
--  Безпечно запускати повторно (upsert за slug).
--
--  max_score = 26 (6 + 6 + 4 + 6 + 4 правильних дій у 5 раундах) —
--  сервер рахує % = score / max_score, а pct_to_grade() → бал 1–12.
--
--  Раунди: 1) яка форма розгалуження потрібна: if / if…else / if…elif…else —
--             6 задач (уроки 18–21), ГР 2;
--          2) or чи and у складній умові — 6 умов (уроки 22–23), ГР 2;
--          3) порахуй за моделлю — 4 задачі з числовою відповіддю
--             (уроки 17, 21, 23), ГР 2;
--          4) тут варто написати проєкт чи швидше без програми —
--             6 ситуацій (урок 17), ГР 3;
--          5) логіка у фільтрах і пошуку — познач 4 правильні з 7
--             (урок 22), ГР 3.
--
--  ⚠️ ЦЯ ГРА — ПЕРША ЧАСТИНА роботи з двох. Друга — власний проєкт учня
--     за одинадцятьма вимогами (рубрика уроку 23). Підсумковий бал за
--     ГР 2 та ГР 3 = СЕРЕДНЄ АРИФМЕТИЧНЕ двох частин, ОКРУГЛЕНЕ ВГОРУ
--     (правило 5 з CLAUDE.md; так само зроблено в 6-А, урок 21).
--     ⛔ НЕ брати «вищу з двох»: тоді одна частина стає безглуздою.
--     ⚠️ Якщо частину не виконано через ТЕХНІЧНУ проблему — ставимо бал
--     за виконану частину й домовляємось про строк, а НЕ середнє з нулем.
--
--  ⚠️ ГРА ОХОПЛЮЄ ДВІ ГРУПИ РЕЗУЛЬТАТІВ ОДРАЗУ:
--       • ГР 2 (створення продуктів і програм) — раунди 1–3, разом 16 балів;
--       • ГР 3 (усвідомлене користування ІТ)   — раунди 4–5, разом 10 балів.
--     Мітки в КТП 8-А: ГР 2 — уроки 19, 21, 23 (поточні); ГР 3 — уроки 17, 22
--     (поточні); урок 24 — ЄДИНА підсумкова точка для обох груп у Темі 3.
--
--  ⚠️ ЩО В ГРІ Є І ЧОГО НЕМАЄ — це важливо сказати класу заздалегідь:
--     ✅ є: вибір форми розгалуження, вибір or/and, чотири невеликі
--        обчислення за моделлю, рішення «писати чи не писати програму»,
--        логіка фільтрів і пошуку;
--     ⛔ немає: означень «за підручником» (що таке кон'юнкція тощо),
--        написання коду з нуля, синтаксису tkinter, таблиць істинності,
--        питань на пріоритет and/or БЕЗ дужок.
--     ⭐ Останнє — свідоме рішення: ми самі вчили ставити дужки завжди,
--        тому таке питання оцінювало б уважність, а не знання.
--
--  ⚠️ Матеріал уроку 13 (шість етапів розв'язування задачі, Mars Climate
--     Orbiter) у грі НЕМАЄ: у КТП цей урок позначено ГР 4, а ГР 4 має свою
--     підсумкову точку — урок 12 (гра bezpeka-tehnika-8). Виняток один:
--     ТЕСТОВІ НАБОРИ (раунд 3, завдання 3) — це вимога 5 рубрики ГР 2,
--     тому вони перевіряються тут.
--
--  ⚠️ Ім'я файлу гри мусить бути alhorytmy-8.html, бо index.html робить
--     перехід на games/<game_slug>.html.
-- ============================================================

insert into public.games (slug, title, description, max_score)
values (
  'alhorytmy-8',
  'Алгоритми та програми',
  'Яка форма розгалуження потрібна, or чи and у складній умові, обчислення за математичною моделлю, коли варто писати програму і логіка у фільтрах та пошуку — ГР 2 та ГР 3, 8 клас, Тема 3',
  26
)
on conflict (slug) do update
  set title       = excluded.title,
      description = excluded.description,
      max_score   = excluded.max_score;

-- ПЕРЕВІРКА: має повернути рядок гри з max_score = 26
select slug, title, max_score, active from public.games where slug = 'alhorytmy-8';
