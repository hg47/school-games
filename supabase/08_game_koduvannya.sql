-- ============================================================
--  ГРА «Кодування даних» — Підсумкова робота ГР 1 (8 клас, урок 7)
--  Куди: Supabase → SQL Editor → New query → вставити ВСЕ → Run.
--  Безпечно запускати повторно (upsert за slug).
--
--  max_score = 18 (4 + 4 + 5 + 3 + 2 правильних дій у 5 раундах) —
--  сервер рахує % = score / max_score, а pct_to_grade() → бал 1–12.
--
--  Раунди: 1) прочитай байт: двійкова → десяткова (4 завдання);
--          2) познач правильні пари «число ↔ байт» (4 правильні з 7);
--          3) переходи між одиницями: Б, б, кБ, МБ (5 завдань);
--          4) довжина коду тексту у Windows-1251 + прийом «поверхи» (3 завдання);
--          5) розмір файлу BMP із вирівнюванням рядків (2 завдання).
--
--  ⚠️ Гра — ЄДИНА частина підсумкової роботи ГР 1 (рубрики немає).
--     Бал сервера і є балом у свідоцтво досягнень. Див. «Критерії оцінювання ГР 1.md».
--
--  ⚠️ Гра перевіряє матеріал уроків 4, 5 і 6 (§ 1.3). Матеріал § 2.1–2.2
--     (стиснення, архіви), який вивчається на тому самому уроці 7, у грі
--     НЕ перевіряється — це було б оцінюванням щойно поясненого.
--
--  ⚠️ Ім'я файлу гри мусить бути koduvannya-8.html, бо index.html робить
--     перехід на games/<game_slug>.html.
-- ============================================================

insert into public.games (slug, title, description, max_score)
values (
  'koduvannya-8',
  'Кодування даних',
  'Двійковий код і байт, число ↔ байт, переходи між одиницями (Б, б, кБ, МБ), довжина двійкового коду тексту, розмір файлу BMP із вирівнюванням — ГР 1, 8 клас',
  18
)
on conflict (slug) do update
  set title       = excluded.title,
      description = excluded.description,
      max_score   = excluded.max_score;

-- ПЕРЕВІРКА: має повернути рядок гри з max_score = 18
select slug, title, max_score, active from public.games where slug = 'koduvannya-8';
